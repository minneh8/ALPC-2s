#include <stdio.h>

int main () {
    int i;
    i = 0;
    while(i <= 5) 
    {
        printf("\n I = %d", i);
        i++;
    }
    
    for (int i = 0; i < 5; i++) {
        printf("\n contador = %d", i);
    }
}